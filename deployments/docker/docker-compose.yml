version: "3.8"

services:
  # PostgreSQL with TimescaleDB
  db:
    image: timescale/timescaledb:latest-pg15
    container_name: p95-db
    environment:
      POSTGRES_USER: p95
      POSTGRES_PASSWORD: p95
      POSTGRES_DB: p95
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init:/docker-entrypoint-initdb.d:ro
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U p95"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis (optional, for caching and pub/sub)
  redis:
    image: redis:7-alpine
    container_name: p95-redis
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # API Server (development with live reload)
  api:
    build:
      context: ../..
      dockerfile: deployments/docker/Dockerfile.api.dev
    container_name: p95-api
    environment:
      - PORT=8080
      - DATABASE_URL=postgres://p95:p95@db:5432/p95?sslmode=disable
      - REDIS_URL=redis://redis:6379
      - JWT_SECRET=change-me-in-production
      - DEPLOYMENT_MODE=self-hosted
      - ADMIN_EMAIL=admin@localhost
      - ADMIN_PASSWORD=admin123
      - DEBUG=true
    volumes:
      - ../..:/app
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  # Web Dashboard
  web:
    build:
      context: ../..
      dockerfile: web/Dockerfile
    container_name: p95-web
    ports:
      - "3000:80"
    depends_on:
      - api
    restart: unless-stopped

volumes:
  pgdata:
  redisdata:
